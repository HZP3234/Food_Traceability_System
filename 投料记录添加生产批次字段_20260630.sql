-- =============================================
-- 投料记录表添加生产批次号字段
-- 用途：使投料记录能关联到具体的生产批次，
--       支持一个生产批次保存多条投料记录
-- 日期：2026-06-30
-- =============================================

ALTER TABLE t_prod_material_input
ADD COLUMN prod_batch_no VARCHAR(64) DEFAULT NULL COMMENT '关联生产批次号';
