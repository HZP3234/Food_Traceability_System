<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foodtraceability.customers.mapper.ScanRecordMapper">

    <resultMap id="BaseResultMap" type="com.foodtraceability.customers.entity.ScanRecord">
        <id column="id" property="id"/>
        <result column="product_batch_no" property="productBatchNo"/>
        <result column="scan_ip" property="scanIp"/>
        <result column="scan_location" property="scanLocation"/>
        <result column="user_id" property="userId"/>
        <result column="scan_time" property="scanTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByProductBatchNo" resultMap="BaseResultMap">
        SELECT * FROM scan_record
        WHERE product_batch_no = #{batchNo}
          AND deleted = 0
        ORDER BY scan_time DESC
    </select>

    <select id="selectByScanLocation" resultMap="BaseResultMap">
        SELECT * FROM scan_record
        WHERE scan_location LIKE CONCAT('%', #{location}, '%')
          AND deleted = 0
        ORDER BY scan_time DESC
    </select>

    <select id="selectRecent" resultMap="BaseResultMap">
        SELECT * FROM scan_record
        WHERE deleted = 0
        ORDER BY scan_time DESC
        LIMIT #{limit}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM scan_record
        WHERE user_id = #{userId}
          AND deleted = 0
        ORDER BY scan_time DESC
        LIMIT #{limit}
    </select>

</mapper>
