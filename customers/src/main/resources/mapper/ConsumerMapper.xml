<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foodtraceability.customers.mapper.ConsumerMapper">

    <resultMap id="BaseResultMap" type="com.foodtraceability.customers.entity.Consumer">
        <id column="consumer_id" property="consumerId"/>
        <result column="consumer_uuid" property="consumerUuid"/>
        <result column="nick_name" property="nickName"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="gender" property="gender"/>
        <result column="region" property="region"/>
        <result column="last_scan_time" property="lastScanTime"/>
        <result column="total_scans" property="totalScans"/>
        <result column="complaint_count" property="complaintCount"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT * FROM t_consumer_info
        WHERE phone = #{phone}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="selectByConsumerUuid" resultMap="BaseResultMap">
        SELECT * FROM t_consumer_info
        WHERE consumer_uuid = #{consumerUuid}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="selectByNickName" resultMap="BaseResultMap">
        SELECT * FROM t_consumer_info
        WHERE nick_name LIKE CONCAT('%', #{nickName}, '%')
          AND is_deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByRegion" resultMap="BaseResultMap">
        SELECT * FROM t_consumer_info
        WHERE region LIKE CONCAT('%', #{region}, '%')
          AND is_deleted = 0
        ORDER BY create_time DESC
    </select>

</mapper>
