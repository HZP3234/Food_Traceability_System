<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foodtraceability.regulation.mapper.AuditLogMapper">

    <resultMap id="BaseResultMap" type="com.foodtraceability.regulation.entity.AuditLog">
        <id column="log_id" property="logId"/>
        <result column="log_uuid" property="logUuid"/>
        <result column="operator_id" property="operatorId"/>
        <result column="operator_name" property="operatorName"/>
        <result column="operation_time" property="operationTime"/>
        <result column="action_type" property="actionType"/>
        <result column="target_id" property="targetId"/>
        <result column="target_desc" property="targetDesc"/>
        <result column="before_data" property="beforeData"/>
        <result column="after_data" property="afterData"/>
        <result column="operation_result" property="operationResult"/>
        <result column="failure_reason" property="failureReason"/>
        <result column="log_hash" property="logHash"/>
        <result column="is_abnormal" property="isAbnormal"/>
        <result column="abnormal_desc" property="abnormalDesc"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <select id="selectByOperatorId" resultMap="BaseResultMap">
        SELECT * FROM t_audit_log WHERE operator_id = #{operatorId} AND is_deleted = 0 ORDER BY operation_time DESC
    </select>

    <select id="selectByActionType" resultMap="BaseResultMap">
        SELECT * FROM t_audit_log WHERE action_type = #{actionType} AND is_deleted = 0 ORDER BY operation_time DESC
    </select>

    <select id="selectByIsAbnormal" resultMap="BaseResultMap">
        SELECT * FROM t_audit_log WHERE is_abnormal = #{isAbnormal} AND is_deleted = 0 ORDER BY operation_time DESC
    </select>

    <select id="selectAllOrderByLogId" resultMap="BaseResultMap">
        SELECT * FROM t_audit_log WHERE is_deleted = 0 ORDER BY log_id ASC
    </select>

    <select id="selectLatestOne" resultMap="BaseResultMap">
        SELECT * FROM t_audit_log WHERE is_deleted = 0 ORDER BY log_id DESC LIMIT 1
    </select>

</mapper>